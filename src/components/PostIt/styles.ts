import { StyleSheet } from 'react-native';
import { theme } from '../../global/styles/theme';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    backgroundColor: theme.postItColors.rioDeJaneiro.primeiro,
    marginBottom: 24,
    padding: 12,
    borderColor: 'black',
    borderWidth: 2,
    borderTopRightRadius: 16,
  },
  text: {

  }
});