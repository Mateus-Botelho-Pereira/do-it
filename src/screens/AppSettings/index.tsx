import React from 'react';
import { styles } from './styles';
import { View, Text } from 'react-native';

export function AppSettings(){
  return (
    <View>
      <Text>handleAppSettings handleAppSettings handleAppSettings</Text>
    </View>
  );
}